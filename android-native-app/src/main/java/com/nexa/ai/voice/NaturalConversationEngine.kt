package com.nexa.ai.voice
import javax.inject.Inject

class NaturalConversationEngine @Inject constructor() {

    /** Process a voice command and return a response string. */
    fun processVoiceCommand(command: String): String = ""
}
