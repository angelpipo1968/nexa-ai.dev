export async function embed(text: string): Promise<number[]> {
    // Mock embedding - replace with actual model call (e.g. OpenAI or Ollama)
    return new Array(768).fill(0).map(() => Math.random());
}
